# qiching.org — xem legacy/project-brain/10-ke-hoach-seo.md (Giai đoạn 0 / D)
# robots.txt quản lý CRAWL, không phải canonicalization và không phải cơ chế bảo mật.
# Việc chặn index môi trường phụ (UAT, mirror pages.dev) làm bằng X-Robots-Tag/redirect
# trong `_headers`/`_redirects`, KHÔNG bằng Disallow ở đây — vì crawler phải đọc được
# response thì mới thấy được noindex.

User-agent: *
Allow: /

# D2: sitemap đã được sinh tự động trong build (`scripts/prerender.mjs`) từ cùng nguồn route
# canonical với prerender. Dùng đường dẫn tương đối để cùng một artifact chạy được ở UAT và
# production.
Sitemap: /sitemap.xml
